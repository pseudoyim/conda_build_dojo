name: dojo_dev
channels:
  - paulyim
  - defaults
dependencies:
  - python >=3.8
# Make sure to get the patched conda from paulyim channel.
  - conda=4.9.2=dojo
  - conda-build
  - gitpython
  - pandas
  - pyyaml
